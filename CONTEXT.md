# 项目语境

## 阅读器术语

### 无缝模式

一种页面排布方式。相邻页面沿阅读方向连续拼接，不为每一页保留固定的整屏空白。它适用于查看由多页共同组成的完整内容。

无缝模式与自动吸附相互独立。开启或关闭其中一项，不会修改另一项。

### 自动吸附

一种滚动停止行为。开启后，滚动会对齐到页面起点；关闭后，读者可以停在页面之间的任意位置。

自动吸附不决定页面是否连续拼接。

### 可见页缩放组

读者在“无缝模式开启、自动吸附关闭”时开始双指缩放，手势开始瞬间与阅读区域有可见交集的全部页面组成可见页缩放组。

组内页面共同缩放和移动，并保持原有接缝。该组在本次缩放结束前保持不变。
